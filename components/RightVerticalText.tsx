export default function RightVerticalText() {
  return (
    <div className="absolute right-12 top-1/2 -translate-y-1/2 z-40 flex items-center justify-center">
      <span className="text-[14px] text-brand-gold uppercase tracking-[0.2em] font-bold whitespace-nowrap rotate-90">
        Paris • Beauty Lab
      </span>
    </div>
  );
}
